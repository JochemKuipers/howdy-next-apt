#!/bin/sh
set -e

# Enrolled face models in /etc/howdy/models are user data and are left
# in place on remove and purge.

#DEBHELPER#

case "$1" in
remove|purge)
	if [ -d /run/systemd/system ]; then
		systemctl daemon-reload >/dev/null 2>&1 || true
	fi
	;;
esac

exit 0
